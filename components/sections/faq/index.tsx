import SectionWrapper from "@/components/shared/section-wrapper";

const Faq = () => {
      return (
            <SectionWrapper title="سوالات متداول">
                  
            </SectionWrapper>
      );
};

export default Faq;